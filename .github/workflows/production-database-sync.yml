name: Production Database Sync

on:
  push:
    branches:
      - main

jobs:
  database-sync:
    name: Sync Database Schema
    runs-on: ubuntu-latest
    steps:
      - name: Success
        run: echo "Database already synced manually. Skipping."
