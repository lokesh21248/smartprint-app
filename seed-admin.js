/**
 * DEPRECATED - DO NOT RUN
 * 
 * This script uses Supabase Auth, which is NOT supported in this project (Clerk is used instead).
 * It also uses a legacy database schema.
 */
console.error("❌ This script is DEPRECATED. We use Clerk for authentication.");
process.exit(1);
